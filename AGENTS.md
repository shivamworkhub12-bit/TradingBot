# Project collaboration rules

- After completing and verifying requested file changes, commit them to the current Git branch and push that branch to its configured remote.
- Never commit `.env`, API keys, access tokens, passwords, or other secrets. Keep safe placeholders in `.env.example` only.
- If tests fail or pushing is blocked, do not hide the failure; report it clearly instead of publishing unverified changes.
